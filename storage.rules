rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Media is read/written through the authenticated server; no public download tokens.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
